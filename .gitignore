.DS_Store
.idea/

# 빌드 산출물
.bin/
dist/
node_modules/
*.tsbuildinfo

# 테스트 픽스처는 make_fixtures.sh 가 만든다
test/fixtures/

# 실제 환경 설정은 저장소에 넣지 않는다 — 실물은 ~/.config/logstitch/ 에 둔다
apps.json
!test/apps.json
inventory.json
inventory.*.json
!inventory.example.json
!test/inventory.sample.test.json
!test/inventory.multi.test.json

# 수집 원본은 고객 키·IP 가 그대로 들어있어 커밋하지 않는다
.runs/
