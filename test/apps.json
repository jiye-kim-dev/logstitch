{
  "_comment": [
    "테스트용 앱 설정. --apps test/apps.json 으로 지정해서 쓴다.",
    "",
    "sample 은 필수 필드 하나 — 기존 파이썬 구현과 출력을 대조하는 데 쓴다",
    "(파이썬은 --field 를 하나만 받는다).",
    "",
    "multi 는 필수 필드 둘 — 원격에서 grep 이 이어붙어 교집합이 나오는지",
    "확인하는 데 쓴다."
  ],
  "apps": {
    "sample": {
      "required": ["rid"]
    },
    "multi": {
      "required": ["rid", "state"],
      "note": "scheduler 픽스처에 rid 와 state 가 같이 찍힌다"
    }
  }
}
